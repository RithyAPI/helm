apiVersion: v1
kind: Secret
metadata:
  name: {{ include "sonarqube.fullname" . }}-db
  labels:
    {{- include "sonarqube.labels" . | nindent 4 }}
type: Opaque
stringData:
  POSTGRES_PASSWORD: "changeme"   # <-- set or overwrite via sealed-secrets/external secret
