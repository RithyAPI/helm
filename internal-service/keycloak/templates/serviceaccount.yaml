{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "keycloak.fullname" .) .Values.serviceAccount.name }}
  labels:
    {{- include "keycloak.labels" . | nindent 4 }}
{{- end }}
